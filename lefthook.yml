pre-commit:
  parallel: true
  commands:
    ruff-check:
      run: scripts/lint.sh
    ruff-format-check:
      run: scripts/format.sh --check

pre-push:
  commands:
    pytest:
      run: uv run pytest
